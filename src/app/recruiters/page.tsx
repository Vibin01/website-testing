import React from "react";
import { RecruitersPage } from "./RecruitersPage";

export const metadata = {
  title: "Recruiter Alignment System for Predictable Hiring | Connect EC",
  description:
    "A recruiter alignment system helps manage interview coordination, prevent no-shows, and capture feedback efficiently. Keep hiring execution predictable with Connect EC.",
};

const page = () => {
  return <RecruitersPage />;
};

export default page;
